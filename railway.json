{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build",
    "outputDir": "dist"
  },
  "deploy": {
    "startCommand": "caddy run --config Caddyfile"
  }
}